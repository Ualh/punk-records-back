database:
  url: "sqlite:///./vault.db"

ollama:
  base_url: "http://127.0.0.1:11434"
  timeout_seconds: 120
  models:
    extraction: "llama3.1:8b"
    quality_check: "qwen2.5:14b"

ingestion:
  upload_dir: "data/uploads"
  chunk_size: 1500
  max_atoms_per_chunk: 5

server:
  host: "0.0.0.0"
  port: 8000
  reload: true
